apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: drm-exporter
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/observability/drm-exporter/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster
