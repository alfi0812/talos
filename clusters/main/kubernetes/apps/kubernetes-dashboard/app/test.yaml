apiVersion: v1
kind: Pod
metadata:
  name: intel-gpu-test
  namespace: default
spec:
  restartPolicy: Never
  containers:
  - name: test
    image: intel/vainfo:latest
    command: ["bash", "-c"]
    args: ["vainfo || ls -l /dev/dri"]
    resources:
      limits:
        "gpu.intel.com/i915": "1"